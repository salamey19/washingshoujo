extends Sprite2D


const green = preload("res://art/2d/player/ONI charges/IMG_2916.PNG")
const red = preload("res://art/2d/player/ONI charges/IMG_2917.PNG")
const yellow = preload("res://art/2d/player/ONI charges/IMG_2918.PNG")

@export_flags("Green", "Yellow", "Red") var text

# Calle"res://art/2d/player/ONI charges/IMG_2917.PNG"d when the node enters the scene tree for the first time.
func _ready() -> void:
	pass


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
